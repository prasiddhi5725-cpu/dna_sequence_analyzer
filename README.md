# DNA Sequence Analyzer

A small tool that takes a DNA sequence and counts the bases (A/T/C/G), 
checks if the sequence is valid, and calculates GC content.

- dna_analyzer_terminal.py — first version, plain terminal input/output
- dna_analyzer_gui.py — same logic, wrapped in a tkinter GUI

Run with: python dna_analyzer_gui.py
